PSet SeedGeneratorParameters = {
   string ComponentName = "DualByEtaTSG" 
   double etaSeparation = 1.5
   PSet TSGsPSet = {
 	# lower than hit fraction
	# better to go with pixel pairs
	{ using pixelPairs },
	# higher than the hit fraction
	# faster to go with hit-less seeding
	{ using forRSIOpxl }
   }	
}
