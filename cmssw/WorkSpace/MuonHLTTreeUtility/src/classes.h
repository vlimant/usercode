#include <vector>
#include <map>
#include <string>

namespace {
  std::map<int,std::vector<double> > recHitsPosition;
  std::map<std::string,double> timingMap;
  std::map<int,std::vector<int> > l3ids;
}
